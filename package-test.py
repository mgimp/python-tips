# import package as pkg # (this syntax imports everything from the package)
from package import simple_math as sm

# print(pkg.add2(1,2)) # (this syntax if we imported the whole package)
print(sm.add2(1,2))
